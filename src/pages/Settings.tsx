export const Settings = () => (
  <div className="panel p-8">
    <h2 className="text-2xl font-bold text-white light:text-slate-900">Settings</h2>
    <p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400 light:text-slate-500">
      Theme, auth, API configuration, notification rules, and rate-limit controls can plug into this shell without changing the main dashboard structure.
    </p>
  </div>
);
